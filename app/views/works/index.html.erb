<div class="center-block" style = "width:400px"><h1>recent u p l o a d s </h1></div>
<br>
<div class="row">
  <% @works.each do |work| %>
    <div class="col-xs-6 col-md-3">
      <a href="#" class="thumbnail">
        <img src="<%= work.media_type %>">
        <%= link_to work.title, work_path(work) %>
      </a>
    </div>
  <% end %>

</div>

<br>
<br>

<div class="btn-group pull-right">
<% if can? :create, Work %>
<%= link_to "add work", new_work_path, class: "btn btn-default btn-sm" %>
<% end %>
</div>
