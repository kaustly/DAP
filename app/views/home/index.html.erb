<h1> w o r k s </h1>

<ul class="list-group">
  <% @recent_works.each do |work| %>
    <li class="list-group-item"><%= link_to work.title, work_path(work) %></li>
  <% end %>
</ul>
<% if can? :create, Work %>
<%= link_to "add work", new_work_path %>
<% end %>
